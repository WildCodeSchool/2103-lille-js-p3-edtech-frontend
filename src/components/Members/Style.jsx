import styled from 'styled-components';

const SMembers = styled.div`
  .Carousel {
    margin: auto;
    width: 100vw;
    height: 20vh;
    .slide {
      height: 15vh;

      img {
        display: block;
        height: 100%;

        margin: auto;
      }
    }
  }
`;
export default SMembers;
